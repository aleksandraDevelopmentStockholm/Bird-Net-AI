# BirdNET Infrastructure Configuration
# Copy this file to .env and update with your values

# Deployment Environment (dev, staging, prod)
BIRDNET_ENV=dev

# AWS Configuration
AWS_REGION=eu-north-1
# AWS_PROFILE=birdnet-deployer  # Optional: uncomment to use a specific AWS profile

# Terraform Workspace (usually same as BIRDNET_ENV)
TF_WORKSPACE=dev
