{
  "name": "birdnet-lambda",
  "version": "1.0.0",
  "description": "AWS Lambda function for BirdNET audio analysis",
  "main": "handler.js",
  "scripts": {
    "build": "pnpm install --prod"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.600.0",
    "@tensorflow/tfjs-node": "^4.22.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "pnpm@9.0.0"
}
